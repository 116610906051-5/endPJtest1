[providers]
python = "3.11"

[phases.setup]
nixPkgs = ["python311", "pip"]

[phases.install]
dependsOn = ["setup"]
cmds = [
  "cd model",
  "pip install --upgrade pip",
  "pip install -r requirements.txt"
]

[start]
cmd = "cd model && uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000}"
